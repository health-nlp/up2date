# Up2Date Task 3 Test Inputs

This package contains candidates for the 15 topics listed as `test` in
`../topic-split.tsv`. Their qrels and version pairs remain private in TIRA
until the submission deadline.