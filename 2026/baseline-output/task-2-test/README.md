# Baseline results for Task 2

The results to task 2 comprise a file `queries.tsv` and `run.txt.gz`. Please upload them to TIRA packaged to one zip file that only contains those two files (e.g., as shown in the `upload-to-tira.zip` file).
